services:
  mcp-server:
    build:
      context: ../..
      dockerfile: packages/mcp/Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - PORT=3000
    restart: unless-stopped
